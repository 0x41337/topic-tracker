/**
 * Practice feature: gamified hit/miss session recording.
 *
 * Self-contained: reads the core store directly and only emits "onFinish"
 * to the host. Embed it in any screen that has a topic id.
 */

export {
    PracticeView,
    type PracticeViewProps,
} from "./components/practice-view"
