/**
 * Dashboard feature: cross-topic performance overview.
 *
 * Props-driven: receives the state snapshot and emits "open topic" intents.
 * Replace it with any aggregate view without touching the explorer.
 */

export {
    DashboardView,
    type DashboardViewProps,
} from "./components/dashboard-view"
